{% from 'elasticSend.j2' import elasticSend %}
# Send all syslog messages to elasticsearch
template(
  name="elasticSearchIndexSyslog"
  type="string"
  string="syslog-%$YEAR%.%$MONTH%.%$DAY%"
)
{{ elasticSend(es, 'elasticSearchIndexSyslog') }}
